package objects;

/**
 * This is basically a marker interface for other interfaces, to check if they
 * are the selection criteria for player resources that work on other objects.
 * This basically come in handy in the UI section where the objects which act on
 * different objects use selection criteria to provide the user a restricted
 * list of possible objects to select from so that that particular object is
 * compatible with the one that is going to be performing some operation on it.
 * 
 * @author rajarshibasu
 *
 */
public interface SelectionCriteria {

}
